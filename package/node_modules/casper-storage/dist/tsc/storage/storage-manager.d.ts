import { IStorage, IStorageConstructable } from "./interfaces";
/**
 * Provides storage mangement integration
 */
export declare class StorageManager {
    private static _storageConstructor;
    /**
     * Get an instance of storage manager
     * @returns
     */
    static getInstance(password: string, throwErrorIfNotAvailable?: boolean): IStorage;
    /**
     * Provides a custom storage
     * @param storage
     */
    static register(storage: IStorageConstructable): void;
}
