import { IStorage } from "../interfaces";
/**
 * Simple wrapper for localStorage of browser
 */
export declare class DefaultStorage implements IStorage {
    private _password;
    constructor(_password: string);
    set(key: string, value: string, password?: string): Promise<void>;
    get(key: string, password?: string): Promise<string>;
    has(key: string): Promise<boolean>;
    remove(key: string): Promise<void>;
    clear(): Promise<void>;
    updatePassword(newPassword: string): Promise<void>;
    getKeys(): Promise<string[]>;
    isAvailable(): boolean;
    protected getStorage(): Storage;
    private encrypt;
    private decrypt;
}
