import { EncryptionType } from "../../../cryptography";
import { Wallet, HDWallet } from "../../../wallet";
import { Hex } from "../../../utils";
/**
 * Casper wallet implementation
 */
export declare class CasperWallet extends Wallet {
    /**
     * Get the public address of current wallet
     */
    getPublicKey(): Promise<string>;
}
/**
 * Casper HD wallet implementation, purpose 44 and using coin-type Casper (506)
 */
export declare class CasperHDWallet extends HDWallet<CasperWallet> {
    private static PATH;
    constructor(masterSeed: Hex, encryptionType?: EncryptionType);
}
