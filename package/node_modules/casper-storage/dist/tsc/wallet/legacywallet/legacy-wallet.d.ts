import { Hex } from "../../utils";
import { BaseWallet } from "../core/base-wallet";
/**
 * Provide detail implementation for a legacy wallet which works with a single private key
 */
export declare class LegacyWallet extends BaseWallet<Hex> {
    private _publicKey;
    getReferenceKey(): string;
    getPrivateKeyByteArray(): Uint8Array;
    getRawPublicKeyByteArray(): Promise<Uint8Array>;
}
