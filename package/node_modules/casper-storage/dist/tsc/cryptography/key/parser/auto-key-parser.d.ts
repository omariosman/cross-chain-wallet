import { KeyValue } from ".";
import { IKeyParser } from "./core";
/**
 * Auto key-parser, try to guess the encryption type from the content and use the properly one
 */
export declare class AutoKeyParser implements IKeyParser {
    convertPEMToPrivateKey(content: string): KeyValue;
    private guessKeyParser;
}
