import { Hex } from "../../utils";
import { IAsymmetricKey } from "./interfaces";
/**
 * Minimal wrapper to ed25519
 */
declare class KeyWrapper implements IAsymmetricKey {
    generatePrivateKey(): Promise<Uint8Array>;
    createPublicKey(privateKey: Hex): Promise<Uint8Array>;
    publicKeyTweakAdd(): Promise<Uint8Array>;
    isValidPrivateKey(): boolean;
    getKeyInPEM(key: Uint8Array, isPrivate: boolean): string;
    /**
    * Inserts the provided `content` and `tag` into a .pem compliant string
    * @param tag The tag inserted on the END line
    * @param content The base-64 PEM compliant private key
    */
    private toPem;
}
export declare const Ed25519: KeyWrapper;
export {};
