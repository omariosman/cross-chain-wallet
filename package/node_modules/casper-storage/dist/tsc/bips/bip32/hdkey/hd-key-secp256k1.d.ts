import { HDKey } from "./hd-key";
export declare class HDKeySecp256k1 extends HDKey {
    protected createNewHDKey(privateKey: Uint8Array, chainCode: Uint8Array, publicKey: Uint8Array): HDKeySecp256k1;
    protected deriveChild(index: number): Promise<HDKey>;
}
